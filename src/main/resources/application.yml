# 应用名称
spring:
  application:
    name: springboot
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    name: defaultDataSource
    url: jdbc:mysql://localhost:3306/mms?serverTimezone=UTC
    username: root
    password: 1135911642
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp

# 应用服务 WEB 访问端口
server:
  port: 8081


#下面这些内容是为了让MyBatis映射

mybatis:
  mapper-locations: classpath:mappers/*xml
  #指定Mybatis的Mapper文件
  type-aliases-package: com.gduf.chb.springboot.bean
  #指定Mybatis的实体目录




